<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-700 flex items-center justify-center h-screen">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">
      <div class="text-center mb-6">
        <!-- Logo icon -->
        <div class="mb-4"></div>
        <h2 class="text-white text-2xl font-bold">Welcome to Y</h2>
      </div>

      <!-- Form -->
      <form action="/login" method="POST">
        <% if (error) { %>
        <p style="color: red;"><%= error %></p>
        <% } %>

        <div class="mb-4">
          <label class="block text-gray-400 text-sm font-medium" for="name">Username</label>
          <input class="mt-1 w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg focus:ring-indigo-500 focus:border-indigo-500" type="text" id="name" name="username" />
        </div>

        <div class="mb-6">
          <label class="block text-gray-400 text-sm font-medium" for="password">Password</label>
          <input class="mt-1 w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg focus:ring-indigo-500 focus:border-indigo-500" type="password" id="password" name="password" />
        </div>

        <!-- Sign in button -->
        <button type="submit" class="w-full bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 px-4 rounded-lg transition">LOG IN</button>
      </form>

      <!-- Sign up link -->
      <div class="text-center mt-6">
        <p class="text-gray-400">
          Don't have an account?
          <a href="/register" class="text-indigo-500 hover:text-indigo-600 font-bold">Sign Up</a>
        </p>
      </div>
    </div>
  </body>
</html>
